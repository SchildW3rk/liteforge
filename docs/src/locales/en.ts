import type { TranslationTree } from 'liteforge/i18n';

const en = {
  // ─── Layout / Nav ────────────────────────────────────────────────────
  nav: {
    foundation:   'Foundation',
    controlFlow:  'Control Flow',
    routing:      'Routing',
    data:         'Data',
    ui:           'UI',
    plugins:      'Plugins',
    tools:        'Tools',

    app:          'App Bootstrap',
    core:         'Signals & Reactivity',
    runtime:      'Components & JSX',
    lifecycle:    'Lifecycle',
    store:        'State Management',
    devtools:     'DevTools',
    controlflow:  'Show / Switch / For',
    router:       'Routing',
    query:        'Data Fetching',
    client:       'HTTP Client',
    form:         'Forms',
    table:        'Tables',
    modal:        'Modal',
    toast:        'Toast',
    tooltip:      'Tooltip',
    calendar:     'Calendar',
    i18n:         'Internationalization',
    admin:        'Admin Panel',
    benchmark:    'Benchmark',
  },

  // ─── Theme Customizer ────────────────────────────────────────────────
  tc: {
    title:        'Appearance',
    accent:       'Accent Color',
    radius:       'Border Radius',
    reset:        'Reset to defaults',
  },

  // ─── Sidebar footer ──────────────────────────────────────────────────
  footer: {
    license:      'MIT License',
    toggleTheme:  'Toggle theme',
    language:     'Language',
  },

  // ─── Overview page ───────────────────────────────────────────────────
  overview: {
    tagline:      'Signals-based · No Virtual DOM · TypeScript-first',
    title:        'LiteForge',
    subtitle:     'A modular frontend framework built around fine-grained reactivity. Direct DOM updates, zero virtual DOM overhead, and a clean component model.',
    getStarted:   'Get started →',
    install:      'Install',
    why:          'Why LiteForge',
    whyItems: {
      noVdom:         'No Virtual DOM',
      noVdomDesc:     'Signals drive direct DOM mutations — no diffing, no reconciler, no wasted renders.',
      noBuildMagic:   'No build magic',
      noBuildMagicDesc: 'Standard Vite plugin, standard TypeScript, standard ESM. Nothing hidden.',
      noOverhead:     'No runtime overhead',
      noOverheadDesc: 'Effects only re-run the exact code that reads changed signals — granular by design.',
    },
    performance:  'Performance',
    perfNote:     'js-framework-benchmark — geometric mean of all metrics (lower is better)',
    perfApprox:   'Approximate — run {cmd} and navigate to {path} for live results.',
    quickstart:   'Quick start',
    quickstartDesc: 'A reactive counter in 15 lines:',
    packages:     'Packages',
    ready:        'Ready to build?',
    readyDesc:    'Set up a new app in under 5 minutes.',
    appBootstrap: 'App Bootstrap →',
    coreConcepts: 'Core concepts',
  },

  // ─── Package descriptions ────────────────────────────────────────────
  pkg: {
    core:     'Signals, computed, effects — the reactive foundation',
    runtime:  'Components, JSX, lifecycle, control flow',
    router:   'Client-side routing with guards and lazy loading',
    query:    'Data fetching with automatic caching and invalidation',
    client:   'TypeScript-first HTTP client with resource CRUD',
    form:     'Form state management with Zod validation',
    table:    'Reactive data grid with sort, filter, pagination',
    modal:    'Portal-based modal system with typed data passing',
    toast:    'Imperative toast notifications with four variants',
    tooltip:  'Portal-based tooltips with auto-positioning and delay',
    calendar: 'Scheduling calendar with 4 views and drag & drop',
    i18n:     'Reactive i18n with lazy locale loading and pluralization',
    admin:    'Full admin scaffold with sidebar, CRUD pages, and auth guards',
  },

  // ─── Core page ───────────────────────────────────────────────────────
  core: {
    title:    'Signals & Reactivity',
    subtitle: 'The reactive foundation of LiteForge. Fine-grained signals that track their dependencies automatically — no subscriptions, no manual cleanup, no VDOM diffing.',
    howItWorks: 'How it works',
    howItWorksDesc: 'Every signal() call creates a reactive cell. When you read a signal inside an effect() or computed(), it registers as a dependency automatically. When the signal changes, only the effects and computed values that depend on it re-run — nothing else.',
    signal:     'signal()',
    signalDesc: 'The building block of reactivity. A signal holds a value and notifies dependents when it changes.',
    computed:   'computed()',
    computedDesc: 'Derives a value from one or more signals. Lazy — only recalculates when a dependency changed and the value is actually read.',
    computedTitle: 'computed() — fullName',
    computedDescEx: 'Derived from two signals',
    effect:     'effect()',
    effectDesc: 'Runs a side effect when dependencies change. Returns a dispose function to stop tracking.',
    batch:      'batch()',
    batchDesc:  'Groups multiple signal updates so dependent effects run only once after all updates complete.',
    liveExample: 'Live example',
    liveTitle:  'signal + computed counter',
    patterns:   'Patterns',
    patternDo:  '✓ Read signals inside effects/JSX expressions',
    patternDoDesc: 'Signals are only tracked when read inside a reactive context (effect, computed, or {code} in JSX).',
    patternDont: '✗ Don\'t read signals outside reactive contexts to "cache" them',
    patternDontDesc: 'Reading a signal outside of an effect or computed snapshot its value at that moment — changes won\'t be tracked.',
    // API row descriptions
    apiSignal:    'Creates a reactive signal with an initial value',
    apiRead:      'Reads the current value — tracked inside effect/computed',
    apiSet:       'Sets a new value directly',
    apiUpdate:    'Updates value via a function: fn receives current value',
    apiComputed:  'Creates a derived signal — recalculates lazily when dependencies change',
    apiDerived:   'Reads the computed value — tracked inside other effects/computed',
    apiEffect:    'Runs fn immediately and re-runs when any signal read inside it changes',
    apiDispose:   'Stop the effect — no more re-runs',
    // Form labels
    firstName:    'First name',
    lastName:     'Last name',
  },

  // ─── Runtime page ────────────────────────────────────────────────────
  runtime: {
    title:      'Components & JSX',
    subtitle:   'The component model and DOM runtime. Create components with {cc}, use JSX for declarative UI, and control rendering with {show}, {for}, and {sw}.',
    subtitlePre:  'The component model and DOM runtime. Create components with',
    subtitleMid:  'use JSX for declarative UI, and control rendering with',
    subtitleAnd:  'and',
    createComponent: 'createComponent()',
    createComponentDesc: 'Defines a component — a factory function that creates a DOM node with reactive bindings.',
    jsx:        'JSX syntax',
    jsxDesc:    'LiteForge JSX compiles to direct DOM operations — no virtual DOM. Key rule: reactive expressions need () => wrappers, event handlers do not.',
    show:       'Show — conditional rendering',
    showDesc:   'Renders children when the when condition is truthy. Unmounts when falsy.',
    showTitle:  'Show',
    showDescEx: 'Toggle visibility',
    for:        'For — list rendering',
    forDesc:    'Renders a list of items reactively. Re-renders the minimal set of items when the array changes.',
    forTitle:   'For',
    forDescEx:  'Press Enter to add items',
    use:        'Plugin injection (use)',
    useDesc:    'Access plugins registered with .use() via the use() function passed to component(). Fully typed via Declaration Merging.',
    lifecycle:  'Async lifecycle (load)',
    lifecycleDesc: 'Use load() for async data fetching. While loading, placeholder() renders. On error, error() renders with a retry() function.',
    // API rows
    apiName:    'Component identifier used in DevTools and HMR',
    apiComponent: 'Render function — receives props, data, setup. Returns a DOM node (JSX)',
    apiSetup:   'Runs before load() — set up signals and handlers that don\'t need async data',
    apiLoad:    'Async data loading — return value is passed as data to component()',
    apiPlaceholder: 'Rendered while load() is in flight',
    apiError:   'Rendered when load() throws — receives error and retry()',
    apiMounted: 'Called after component is mounted to the DOM',
    apiDestroyed: 'Called when component is removed from the DOM',
  },

  // ─── App page ────────────────────────────────────────────────────────
  app: {
    title:      'App Bootstrap',
    subtitle:   '{createApp} bootstraps a LiteForge application. It initializes stores, installs plugins in order (awaiting async installs), then mounts the root component. The builder is {thenable} — top-level {await} works without an explicit {mount} call.',
    subtitlePre:    'bootstraps a LiteForge application. It initializes stores, installs plugins in order (awaiting async installs), then mounts the root component. The builder is',
    subtitleTopLevel: 'top-level',
    subtitleMid:    'works without an explicit',
    subtitleSuffix: 'call.',
    minimal:    'Minimal bootstrap',
    full:       'Full bootstrap',
    fullDesc:   'Real-world main.tsx with all plugins chained. Plugins install in registration order; async installs are awaited before mount.',
    plugins:    'Plugin system',
    pluginsDesc: 'A plugin is a factory returning a LiteForgePlugin. Plugins provide values, consume other plugins via resolve(), and return cleanup functions.',
    use:        'use() — consuming plugins in components',
    useDesc:    'Any value registered via provide() is accessible in component setup() via use(). Fully typed via Declaration Merging on PluginRegistry.',
    context:    'context — static app values',
    contextDesc: 'Pass arbitrary values at app level. Useful for environment config, feature flags, or shared constants that don\'t need a full plugin.',
    stores:     'stores — pre-initialized state',
    storesDesc: 'Stores passed to createApp() are initialized before any component mounts. All components get the same singleton instance via use().',
    debug:      'debug mode & $lf',
    debugDesc:  'debug: true exposes window.$lf for console inspection. Required for @liteforge/devtools to connect.',
    thenable:   'Thenable & async mount',
    thenableDesc: 'AppBuilder implements .then()/.catch() — it behaves like a Promise. All three patterns below are equivalent.',
    destroy:    'app.destroy()',
    destroyDesc: 'Unmounts the app and runs all plugin cleanups in reverse registration order. Useful in tests or SPAs that swap roots.',
    apiOptions: 'createApp() options',
    apiBuilder: 'AppBuilder methods',
    apiPlugin:  'LiteForgePlugin',
    apiCtx:     'PluginContext (install argument)',
    apiApp:     'App instance',
    // API rows
    apiRoot:    'Root component of the application',
    apiTarget:  'CSS selector or element to mount into',
    apiStores:  'Stores to initialize before mount — available via use() in all components',
    apiContext: 'Arbitrary values provided to all components via use()',
    apiDebug:   'Exposes window.$lf for console debugging. Required for @liteforge/devtools.',
    builderUse: 'Register a plugin. Returns the builder for chaining. Throws after .mount().',
    builderUseDev: 'Register a plugin only in development (import.meta.env.DEV). Factory is a function returning Promise<LiteForgePlugin>. Tree-shaken in prod.',
    builderMount: 'Install all plugins (in registration order, awaiting async installs) then mount the root component. Returns the App instance.',
    builderThen:  'Thenable — delegates to .mount().then(fn). Enables top-level await without explicit .mount() call.',
    builderCatch: 'Thenable — delegates to .mount().catch(fn).',
    pluginName:   'Unique plugin identifier. Duplicate names throw before any install() runs.',
    pluginInstall: 'Called once during mount. Async is supported — app waits for resolution. Return a cleanup function to run on app.destroy().',
    ctxProvide:   'Register a value accessible via use(key) in all components.',
    ctxResolve:   'Access a value provided by another plugin. Throws if the key is not yet registered.',
    ctxTarget:    'The resolved mount target element.',
    appDestroy:   'Unmount the root component and run all plugin cleanup functions in reverse registration order.',
  },

  // ─── i18n page ───────────────────────────────────────────────────────
  i18n: {
    title:      'Internationalization',
    subtitle:   'Signals-based i18n plugin. Lazy-loaded locale files, dot-notation keys, interpolation, pipe-based pluralization, fallback locale, and localStorage persistence. No re-render on locale switch — only the text nodes that read {t} update.',
    subtitlePre:  'Signals-based i18n plugin. Lazy-loaded locale files, dot-notation keys, interpolation, pipe-based pluralization, fallback locale, and localStorage persistence. No re-render on locale switch — only the text nodes that read',
    subtitleSuffix: 'update.',
    setup:      'Plugin setup',
    setupDesc:  'Register i18nPlugin before .mount() — it awaits the initial locale before the app renders, preventing any flash of untranslated keys.',
    usage:      'Using translations',
    usageDesc:  'Access the i18n API via use(\'i18n\') in any component\'s setup(). t() is a plain function — wrap it in () => for reactive JSX.',
    localeFiles: 'Locale files',
    localeFilesDesc: 'Translation trees are plain TypeScript objects — no special format, no CLI needed. Use satisfies TranslationTree for type checking.',
    interpolation:     'Interpolation',
    interpolationDesc: 'Embed dynamic values in translation strings using {placeholder} syntax. Pass them as the second argument to t().',
    plural:     'Pluralization',
    pluralDesc: 'Pipe-separated strings. 2 parts = singular|plural. 3 parts = zero|one|many. Pass count as the third argument to t().',
    pluralization:      'Pluralization',
    pluralizationDesc:  'Use pipe-separated strings for plural forms. Pass count as the third argument to t(). Two-part or three-part forms supported.',
    fallback:   'Fallback locale',
    fallbackDesc: 'When a key is missing in the current locale, the plugin automatically falls back to the fallback locale. No silent undefined — the user always sees a string.',
    vsParaglide:     '@liteforge/i18n vs. Paraglide',
    vsParaglideDesc: 'Paraglide compiles messages at build time for maximum tree-shaking. @liteforge/i18n is fully runtime and signal-based — locale switching is instant with no page reload, no build step, no codegen. Trade-off: Paraglide eliminates unused strings per-page; @liteforge/i18n lazy-loads entire locale files.',
    live:       'Live example',
    liveDesc:   'Click the locale buttons — only the bound text nodes update, no component re-render.',
    liveTitle:  'Locale switch · interpolation · pluralization · fallback',
    // API row descriptions
    apiDefaultLocale: 'Locale loaded on startup (or from localStorage if persist: true)',
    apiFallbackLocale: 'Loaded in parallel; used when a key is missing in the current locale',
    apiLoad:    'Async loader — return the raw translation object for the given locale',
    apiPersist: 'Save locale choice to localStorage; restore on next visit',
    apiStorageKey: 'localStorage key used for persistence',
    apiLocale:  'Signal — current locale. Auto-subscribes callers inside effects / JSX.',
    apiSetLocale: 'Load translations for the new locale, update signal atomically via batch()',
    apiT:       'Translate a dot-notation key. Supports {param} interpolation and | pipe pluralization.',
  },

  // ─── Store page ──────────────────────────────────────────────────────
  store: {
    title:        'State Management',
    subtitle:     'Signal-based global state management. Define state, computed getters, and async actions in one place. Zero boilerplate — no reducers, no dispatches.',
    defineStore:  'defineStore()',
    defineStoreDesc: 'Pass a unique name, state shape, optional getters, and actions. Each state key becomes a Signal automatically.',
    instance:     'Store instance',
    instanceDesc: 'The returned store exposes state signals, getters, and actions directly on the object.',
    live:         'Live example',
    liveDesc:     'Counter store with getters, actions, and a 5-entry action history.',
    liveTitle:    'Counter store',
    liveDescEx:   'Uses defineStore with getters + actions',
    plugins:      'Plugins',
    pluginsDesc:  'Plugins intercept actions and state changes globally. Use defineStorePlugin() to create one.',
    timeTravel:   'Time-travel',
    timeTravelDesc: 'DevTools integration enables time-travel debugging — rewind state to any previous snapshot.',
  },

  // ─── Router page ─────────────────────────────────────────────────────
  router: {
    title:        'Routing',
    subtitle:     'Client-side routing with history modes, nested routes, lazy loading, typed guards, and middleware. Zero dependencies — built on the native History API.',
    setup:        'Setup',
    setupDesc:    'Create a router with route definitions and a history mode. Pass it to createApp() — the routerPlugin() handles wiring.',
    nested:       'Nested routes',
    nestedDesc:   'Parent components render <RouterOutlet /> to display matched children. Deeply nested routes work recursively.',
    lazy:         'Lazy loading',
    lazyDesc:     'Provide a dynamic import function as the component — routes are loaded on demand. Works for any named or default export.',
    link:         'Link & RouterOutlet',
    linkDesc:     'Link renders a semantic <a> tag with active-class support. RouterOutlet marks where matched routes render.',
    useRouter:    'use("router") — reactive params',
    useRouterDesc: 'Access the router instance via use("router") in any component. currentRoute() is a Signal — reactive by default.',
    liveTitle:    'Simulated router',
    liveDesc:     'Tab navigation, nested param route, and programmatic navigation — all without a real browser router.',
    guards:       'Route guards',
    guardsDesc:   'Guards run before navigation completes. Return true to proceed, a path string to redirect, or false to cancel.',
    middleware:   'Middleware',
    middlewareDesc: 'Middleware runs around every navigation in the order registered. Use next() to advance the pipeline.',
    typedRoutes:      'Typed Routes',
    typedRoutesDesc:  'Pass your routes array as const — navigate() will only accept valid paths. TypeScript errors on typos, wrong paths, or missing params.',
    typedRoutesPhase1: 'Phase 1 — Path safety',
    typedRoutesPhase1Desc: 'With as const, navigate() is constrained to paths that match a known route pattern.',
    typedRoutesPhase2: 'Phase 2 — Param safety',
    typedRoutesPhase2Desc: 'Use the path pattern as first arg and a typed params object as second. TypeScript infers the required keys from the pattern.',
    typedRoutesNote:  'Known limitation: optional params (:param?) are treated as required in Phase 2. Full optional support is planned.',
    viewTransitions:      'View Transitions',
    viewTransitionsDesc:  'Wrap route changes in the native View Transitions API or provide custom enter/leave hooks for CSS animations.',
    viewTransitionsNote:  'useViewTransitions falls back gracefully in browsers that do not support the View Transitions API.',
    lazyWrapper:     'lazy() wrapper',
    lazyWrapperDesc: 'Use the lazy() helper for preload-on-hover and custom delay/timeout per route.',
    lazyChildren:    'Lazy route subtrees',
    lazyChildrenDesc: 'Load entire route subtrees on demand — ideal for admin panels or large feature areas.',
  },

  // ─── Query page ──────────────────────────────────────────────────────
  query: {
    title:        'Data Fetching',
    subtitle:     'Reactive data fetching with automatic caching, deduplication, and background revalidation. Eliminates manual loading/error state — your components stay clean.',
    problem:      'The problem with manual fetching',
    createQuery:  'createQuery()',
    createQueryDesc: 'Define a query with a cache key and fetch function. The query auto-fetches on first read and re-fetches when reactive keys change.',
    reactiveKeys: 'Reactive cache keys',
    reactiveKeysDesc: 'When the key is a function, it is tracked as a reactive dependency. Changing a signal that the key reads triggers an automatic refetch.',
    createMutation: 'createMutation()',
    createMutationDesc: 'Mutations wrap write operations. Pass invalidate keys to automatically refetch related queries after success.',
    live:         'Live example',
    liveDesc:     'Navigate between posts — cached entries show instantly, new ones show a loading state.',
    liveTitle:    'Reactive query with pagination',
  },

  // ─── Form page ───────────────────────────────────────────────────────
  form: {
    title:        'Forms',
    subtitle:     'Signal-based form state with Zod validation. Per-field error signals, array fields, and configurable validation timing — no controlled input ceremony.',
    createForm:   'createForm()',
    createFormDesc: 'Define a form with a Zod schema, initial values, and a submit handler. Returns an instance with per-field accessors.',
    fields:       'Field accessors',
    fieldsDesc:   'form.field(name) returns a FieldInstance with reactive value, error, touched, and dirty signals plus imperative methods.',
    validation:   'Zod schema validation',
    validationDesc: 'Pass any Zod schema to createForm(). Validation runs per-field on blur and re-validates on change after the first error.',
    arrays:       'Array fields',
    arraysDesc:   'form.array(name) returns an ArrayFieldInstance for dynamic field lists — append, remove, and access per-item fields.',
    state:        'Form state',
    live:         'Live example',
    liveTitle:    'Login form',
    liveDesc:     'Blur a field to validate. Fix the error and the warning clears on each keystroke.',
    liveArray:    'Array fields example',
    liveArrayDesc: 'Add, remove, and edit line items. The computed total updates reactively.',
    liveArrayTitle: 'Invoice line items',
    liveArrayDescEx: 'form.array() with per-item fields and a computed total',
  },

  // ─── Table page ──────────────────────────────────────────────────────
  table: {
    title:        'Tables',
    subtitle:     'Reactive data grid with sort, filter, pagination, and row selection. Zero DOM diffing — only the changed cells update.',
    createTable:  'createTable()',
    createTableDesc: 'Pass a reactive data source and column definitions. Returns a table instance with Root() for rendering.',
    columns:      'Column definitions',
    columnsDesc:  'Each column needs a key and header. Add sortable, filterable, or a custom cell renderer. Use _actions for a row-actions column.',
    filters:      'Filters',
    filtersDesc:  'Add a filter function per column. The table exposes setFilter() and clearFilters() for programmatic control.',
    selection:    'Row selection',
    selectionDesc: 'Enable selection with mode: "single" or "multi". The selected signal holds the current selection.',
    state:        'Table state',
    stateDesc:    'Access or set sort, page, and search state programmatically. All state is reactive — bind it to external UI controls.',
    styling:      'Styling system',
    stylingDesc:  'The table uses a 3-layer cascade. Each layer is independent and composable.',
    styleTokens:  'Style tokens (styles: {})',
    styleTokensDesc: 'Pass a styles object to override CSS custom properties scoped to this table instance only.',
    live:         'Live example — styles tokens',
    liveDesc:     'The same table data with custom style tokens applied.',
    liveTitle:    'Patients table — custom tokens',
    tailwind:     'Live example — Tailwind + unstyled',
    tailwindDesc: 'Set unstyled: true and use the classes option to apply Tailwind utilities.',
    tailwindTitle: 'Patients table — Tailwind classes',
  },

  // ─── Modal page ──────────────────────────────────────────────────────
  modal: {
    title:        'Modal',
    subtitlePre:  'Portal-based modal system. Modals live outside your app root — no z-index wars. Built-in presets',
    subtitleAnd:  'and',
    subtitleSuffix: 'handle common use cases with zero boilerplate.',
    setup:        'Setup — ModalProvider',
    setupDesc:    'Mount ModalProvider once, next to your app root. It renders all open modals into a portal and manages focus trapping.',
    createModal:  'createModal()',
    createModalDesc: 'Returns a modal instance with open(), close(), toggle(), and destroy(). The component prop is a factory that returns a Node.',
    presets:      'Presets',
    presetsDesc:  'confirm(), alert(), and prompt() are Promise-based presets built on createModal(). No configuration needed for common dialogs.',
    live:         'Live example',
    liveDesc:     'The modal and presets below are real — they run in this page.',
    liveTitle:    'Modal & presets',
    liveDescEx:   'createModal + confirm + alert + prompt',
  },

  // ─── Toast page ──────────────────────────────────────────────────────
  toast: {
    title:        'Toast',
    subtitle:     'Imperative toast notifications with four variants. Stacks vertically, auto-dismisses, and supports promise-based feedback.',
    setup:        'Setup — ToastProvider',
    setupDesc:    'Mount ToastProvider once at app level. It renders the toast stack into a portal outside your app root.',
    basic:        'Basic usage',
    promise:      'Promise toast',
    promiseDesc:  'toast.promise() shows loading → success/error states automatically. Pass a Promise and three message strings.',
    position:     'Position',
    cssVars:      'CSS custom properties',
    cssVarsDesc:  'Override any CSS variable on :root or a parent element to theme the toast stack.',
    pluginOptions: 'Plugin options',
    api:          'toast API',
    toastOptions: 'Toast options',
    liveTitle:    'Toast variants',
  },

  // ─── Tooltip page ────────────────────────────────────────────────────
  tooltip: {
    title:        'Tooltip',
    subtitlePre:  'Portal-based tooltips appended to',
    subtitleMid:  "so they're never clipped by",
    subtitleMid2: 'parents. Supports auto-positioning, hover delay, and a',
    subtitleSuffix: 'guard for conditional display.',
    basic:        'Basic usage',
    ref:          'ref callback pattern',
    refDesc:      'In component(), use a ref callback to get the element reference and attach tooltip() directly.',
    showWhen:     'showWhen — conditional display',
    showWhenDesc: 'showWhen is evaluated on every pointerenter. Perfect for sidebars, collapsed menus, or any element with dynamic context.',
    delay:        'Hover delay',
    delayDesc:    'Pass delay in ms to debounce the tooltip appearance. Prevents flickering on quick mouse-overs.',
    cleanup:      'Cleanup',
    cleanupDesc:  'tooltip() returns a cleanup function. Call it to detach event listeners and hide any active tooltip — useful in onCleanup().',
    gotcha:       '⚠ Gotcha — native title attribute',
    component:    'Tooltip component',
    componentDesc: 'The Tooltip component wraps a child element in a display:contents span and calls tooltip() internally.',
    auto:         'Auto-attach via data attribute',
    autoDesc:     'autoTooltips() scans the DOM for elements with data-tooltip and attaches tooltip() automatically. Re-call after dynamic inserts.',
    cssVars:      'CSS custom properties',
    cssVarsDesc:  'Override any CSS variable to theme the tooltip globally or on a specific parent.',
    api:          'tooltip() function',
    optionsApi:   'Options',
    componentApi: 'Tooltip component props',
    liveBasicTitle:  'Positions demo',
    liveShowWhenTitle: 'showWhen demo',
    liveDelayTitle:  '150ms delay',
    liveCleanupTitle: 'Mount / unmount',
  },

  // ─── Calendar page ───────────────────────────────────────────────────
  calendar: {
    title:        'Calendar',
    subtitle:     'Scheduling calendar with Day, Week, Month, and Agenda views. Drag & drop, event resize, resource columns, recurring events, and dark mode — zero dependencies.',
    live:         'Live example',
    liveDesc:     'Full calendar with drag & drop, resource columns, and all four views.',
    liveTitle:    'Calendar',
    liveDescEx:   'Drag events to reschedule. Resize from the bottom edge.',
    createCalendar: 'createCalendar()',
    createCalendarDesc: 'Pass events, view, and optional resources. Returns a calendar instance with Toolbar() and Root() render methods.',
    resources:    'Resource columns',
    resourcesDesc: 'Pass a resources array to enable the Day view resource columns — useful for multi-staff or multi-room scheduling.',
    eventHandlers: 'Event handlers',
    eventHandlersDesc: 'Respond to drag, resize, and slot click events. All handlers receive typed arguments.',
    recurring:    'Recurring events',
    recurringDesc: 'Add a recurrence rule to any event. The calendar expands recurring events into occurrences automatically.',
    navigation:   'Programmatic navigation',
    navigationDesc: 'Call next(), prev(), today(), setView(), and toggleResource() on the calendar instance.',
  },

  // ─── Control flow page ───────────────────────────────────────────────
  controlflow: {
    title:        'Control Flow',
    subtitle:     'Show, Switch/Match, and For — fine-grained conditional and list rendering with minimal DOM churn.',
    reactiveExpr: 'Reactive expressions in JSX',
    reactiveExprDesc: 'Any JSX child wrapped in () => is tracked as a reactive expression. When the signal it reads changes, only that text node or element updates.',
    show:         'Show',
    showDesc:     'Mounts children when the when condition is truthy, unmounts when falsy. Use fallback for the else branch.',
    liveShowTitle: 'Show + fallback',
    switchMatch:  'Switch / Match',
    switchMatchDesc: 'Switch evaluates Match conditions in order and renders the first truthy one. Use fallback for the default case.',
    liveSwitchTitle: 'Switch / Match',
    for:          'For',
    forDesc:      'For renders a keyed list. Provide a key function to help the runtime reuse existing DOM nodes when the array changes.',
    liveForTitle: 'For — dynamic list',
    whenToUse:    'When to use which',
    patterns:     'Common patterns',
  },

  // ─── Lifecycle page ──────────────────────────────────────────────────
  lifecycle: {
    title:        'Lifecycle',
    subtitle:     'The full component lifecycle — from setup through async load, mount, reactive updates, and teardown.',
    diagram:      'Lifecycle diagram',
    fullExample:  'Full example',
    fullExampleDesc: 'A component that uses every lifecycle hook — setup, load, placeholder, component, mounted, and destroyed.',
    demo:         'Interactive demo',
    demoDesc:     'Mount and unmount the child component to observe lifecycle events in real time.',
    liveTitle:    'Lifecycle events log',
    onCleanup:    'onCleanup()',
    onCleanupDesc: 'Register a cleanup function inside an effect. Runs before each re-run and when the component is destroyed.',
    tooltipCleanup: 'Real-world: tooltip + onCleanup',
    tooltipCleanupDesc: 'tooltip() returns a cleanup function — pass it directly to onCleanup() to remove listeners on unmount.',
    mountUnmount: 'onMount() / onUnmount()',
    mountUnmountDesc: 'Convenience wrappers callable from inside component(). Equivalent to the mounted() and destroyed() lifecycle hooks.',
    cleanupVsUnmount: 'onCleanup vs onUnmount',
    cleanupVsUnmountDesc: 'onCleanup is effect-scoped and runs on every effect re-run. onUnmount is component-scoped and runs once.',
    hooks:        'Lifecycle hook API',
    utils:        'Lifecycle utility API',
  },

  // ─── DevTools page ───────────────────────────────────────────────────
  devtools: {
    title:        'DevTools',
    subtitle:     'In-app debug panel with 5 tabs: signal inspector, store time-travel, router log, component tree, and performance timeline.',
    integration:  'Integration',
    integrationDesc: 'Register devtoolsPlugin() in createApp(). The panel opens with the configured keyboard shortcut.',
    config:       'Configuration',
    configDesc:   'All options are optional — defaults work out of the box.',
    tabs:         'Panel tabs',
    tabsDesc:     'Five tabs covering every layer of the framework.',
    timeTravel:   'Store time-travel',
    timeTravelDesc: 'Click any action in the Stores tab to rewind state. The runtime calls $restore() on the store — all reactive effects re-run.',
    standalone:   'Standalone (no createApp)',
    standaloneDesc: 'Use createDevTools() to attach the panel without createApp() — useful for plain scripts or micro-frontends.',
  },

  // ─── Client page ─────────────────────────────────────────────────────
  client: {
    title:        'HTTP Client',
    subtitle:     'TypeScript-first HTTP client with interceptors, middleware, typed resource CRUD, and optional query integration.',
    progressiveDx: 'Progressive DX',
    progressiveDxDesc: 'Four levels of abstraction — choose the level that fits your use case. Every level builds on the previous one.',
    createClient: 'createClient()',
    createClientDesc: 'Configure base URL, default headers, timeout, and retry policy. Returns a client instance with shorthand HTTP methods.',
    resource:     'resource() — typed CRUD',
    resourceDesc: 'client.resource<T>(name) returns a ResourceClient with getList, getOne, create, update, delete, action, and custom methods.',
    queryClient:  'QueryClient integration',
    queryClientDesc: 'Pass { createQuery, createMutation } to createClient() to get a QueryClient — resource() returns QueryResource with use* reactive methods.',
    errors:       'ApiError & error handling',
    errorsDesc:   'Failed responses throw ApiError with status, message, and parsed data. Handle globally via an interceptor or locally with try/catch.',
    liveTitle:    'Fetch demo',
    liveDesc:     'Uses the native fetch API directly. The real @liteforge/client wraps this with ApiError, retries, and interceptors.',
    interceptors: 'Interceptors',
    interceptorsDesc: 'Add request/response interceptors. Request interceptors run FIFO; response interceptors run LIFO (outermost-last).',
    middleware:   'Middleware',
    middlewareDesc: 'Middleware is a lower-level pipeline for composable fetch logic. Useful for logging, timing, and retry strategies.',
  },

  // ─── Admin page ──────────────────────────────────────────────────────
  admin: {
    title:        'Admin Panel',
    subtitle:     'Full admin scaffold generated from resource definitions. Sidebar, CRUD list/detail/form pages, auth guards, and a plugin system — all from a single defineResource() call.',
    defineResource: 'defineResource()',
    defineResourceDesc: 'Define a resource with its API endpoint, column definitions, form layout, and hooks. registerResource() adds it to the global registry.',
    routerIntegration: 'Router integration',
    routerIntegrationDesc: 'buildAdminRoutes() generates all CRUD routes from registered resources. Spread them into your existing router config.',
    pluginSetup:  'adminPlugin()',
    pluginSetupDesc: 'Register adminPlugin() in createApp() to inject the sidebar layout, CSS, and navigation into your app.',
    fieldTypes:   'Field types',
    fieldTypesDesc: 'Admin fields map to standard HTML inputs. Use renderCell / renderForm for fully custom rendering.',
    hooks:        'Lifecycle hooks',
    hooksDesc:    'Hooks intercept CRUD operations before or after the API call. Return false from beforeDestroy to cancel deletion.',
    customCells:  'Custom cell renderers',
    customCellsDesc: 'Use renderCell to return a DOM node for list/detail cells. Add rowActions for per-row buttons.',
    relations:    'Relation fields',
    relationsDesc: 'The relation field type renders a select populated from another registered resource. Specify the resource name and the field to use as the option label.',
  },

  // ─── Generic / shared ────────────────────────────────────────────────
  shared: {
    preview:    'Preview',
    code:       'Code',
    copy:       'Copy',
    copied:     'Copied!',
    learnMore:  'Learn more',
  },
} satisfies TranslationTree;

/** Canonical shape — de.ts must satisfy this type exactly. */
export type DocsTranslations = typeof en;

export default en;
